import SwiftUI
import AIKit

// Intentionally empty for now.
//
// This target will host SwiftUI-first reusable components that integrate natively
// with AIKit types (ChatStore, ChatMessage, streaming, tools, approvals, etc.).
