import SwiftUI

/// Public alias matching Vercel AI Elements' `Confirmation` naming. Forwards
/// directly to ``ApprovalBanner`` — the requested/approved/rejected approval UI.
public typealias Confirmation = ApprovalBanner

/// Public alias for ``ApprovalBannerState`` to match the `Confirmation` naming.
public typealias ConfirmationState = ApprovalBannerState
