# Vendored References

This directory holds vendored upstream repositories used for behavioral parity and reference.

Contents are copied into new worktrees via `gt.json` post-create steps and are not committed.
